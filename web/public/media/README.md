# Image slots

Drop files here, then set `src` on the matching `media` entry in `content/en.ts` + `content/vi.ts`
(e.g. `src: "/media/mint.webp"`). Until then `<Figure>` renders a designed placeholder.

Filenames, ratios and art direction: `context/media-plan.md`.
